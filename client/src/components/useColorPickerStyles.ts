import { makeStyles } from "@fluentui/react-components";

export const useColorPickerStyles = makeStyles({
    button: {
        gap: "10px",
        justifyContent: "start",
    },
    colorPickerContainer: {
        display: "flex",
        gap: "10px",
    },
});
